{
  "name": "@repo/icons",
  "version": "0.0.0",
  "private": true,
  "exports": {
    ".": "./dist/index.ts"
  },
  "scripts": {
    "dev": "rm -rf dist && svgr --out-dir dist --ignore-existing -- asserts"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@svgr/cli": "^8.1.0"
  }
}
